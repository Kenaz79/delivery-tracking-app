# Leave unset to run the dashboard against built-in demo data (no backend required).
# Set this once the backend is deployed to switch to live data, e.g.:
# VITE_API_URL=http://localhost:4000
VITE_API_URL=
